apiVersion: apps/v1

kind: Deployment

metadata:

  name: app-3

  labels:

    app: app-3

    version: v1

spec:

  replicas: 2

  selector:

    matchLabels:

      app: app-3

  template:

    metadata:

      labels:

        app: app-3

        version: v1

    spec:

      containers:

      - name: nginx

        image: nginx:1.21-alpine

        ports:

        - containerPort: 80

        resources:

          requests:

            memory: "64Mi"

            cpu: "50m"

          limits:

            memory: "128Mi"

            cpu: "100m"

---

apiVersion: v1

kind: Service

metadata:

  name: app-3-service

spec:

  selector:

    app: app-3

  ports:

  - port: 80

    targetPort: 80

